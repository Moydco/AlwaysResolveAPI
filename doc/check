{
  "$schema": "http://json-schema.org/draft-03/schema",
  "properties": {
    "name": {
      "description": "a mnemonic string",
      "required": false,
      "type": "string",
      "default": ""
    },
    "ip": {
      "description": "the IP Address to check",
      "required": true,
      "type": "string",
      "default": ""
    },
    "check": {
      "description": "the type of check by nagios-plugins or PASSIVE for a passive check",
      "required": true,
      "type": "string",
      "default": "check_ping"
    },
    "check_args": {
      "description": "the arguments of nagios plugin check",
      "required": false,
      "type": "string",
      "default": ""
    },
    "soft_to_hard_to_enable": {
      "description": "the number of consecutive checks OK to consider an host UP",
      "required": false,
      "type": "integer",
      "default": 3
    },
    "soft_to_hard_to_disable": {
      "description": "the number of consecutive checks OK to consider an host DOWN",
      "required": false,
      "type": "integer",
      "default": 3
    },
    "enabled": {
      "description": "if is enabled or disabled",
      "required": false,
      "type": "boolean",
      "default": true
    },
    "reports_only": {
      "description": "check the server but don't act on DNS configuration",
      "required": false,
      "type": "boolean",
      "default": false
    },
    "soft_status": {
      "description": "only for internal use",
      "required": false,
      "type": "boolean",
      "default": true
    },
    "hard_status": {
      "description": "only for internal use",
      "required": false,
      "type": "boolean",
      "default": true
    },
    "soft_count": {
      "description": "only for internal use",
      "required": false,
      "type": "integer",
      "default": 0
    },
    "hard_count": {
      "description": "only for internal use",
      "required": false,
      "type": "integer",
      "default": 999
    },
    "last_bill": {
      "description": "last time this service was billed",
      "required": false,
      "type": "string",
      "default": "Thu, 30 Oct 2014"
    },
    "admin_enabled": {
      "description": "only for internal use",
      "required": false,
      "type": "boolean",
      "default": true
    }
  }
}